def common_prifix(str):
    prefix=str[0]
    for i in range(1,len(str)):
        while not str[i].startswith(prefix):
            prefix=prefix[:-1]
        return prefix

str=["flying","fliying","flow"]
print(common_prifix(str))
